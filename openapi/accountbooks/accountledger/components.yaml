AccountLedgerEntryItem:
  type: object
  properties:
    date:
      type: string
    ledgerName:
      type: string
    vtype:
      type: string
    debitAmt:
      type: number
    creditAmt:
      type: number
    balance:
      type: number
    crdr:
      $ref: '../../accountmaster/ledger/components.yaml#/AccountEntryType'

AccountLedgerSearchableField:
  type: string
  enum: [VTYPE]

AccountLedgerPageResponse:
  type: object
  properties:
    data:
      type: array
      items:
        $ref: '#/AccountLedgerEntryItem'
    pagination:
      $ref: '../../common/components.yaml#/PaginationMeta'
